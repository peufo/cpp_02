#include <iostream>
#include "Point.hpp"

static bool bsp(Point const a, Point const b, Point const c, Point const point)
{

}

static void bspTest(Point const a, Point const b, Point const c, Point const point)
{
	if (bsp)
}

int main( void )
{



	return 0;
}